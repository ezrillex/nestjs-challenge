import { CreateCategoryInput } from './create-category-input';

describe('CreateCategoryInput', () => {
  it('should be defined', () => {
    expect(new CreateCategoryInput()).toBeDefined();
  });
});
